import { TxnEditDirective } from './txn-edit.directive';

describe('TxnEditDirective', () => {
  it('should create an instance', () => {
    const directive = new TxnEditDirective();
    expect(directive).toBeTruthy();
  });
});
