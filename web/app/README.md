# Angular Application

This is the entrypoint to the Angular application.

Global styles are in the main `styles.css`. Google Fonts are installed as well for
Angular Material Icons support.

As a SPA, all routing is done within the app.
